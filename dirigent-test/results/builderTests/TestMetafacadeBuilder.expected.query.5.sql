SELECT 
	E.EMP_ID AS "EMPLOYEE_ID",
	E.EMP_FIRST_NAME||' '||E.LAST_NAME AS "EMPLOYEE_NAME",
	D.DEPT_ID AS "DEPARTMENT_ID",
	D.DEPT_NAME AS "DEPARTMENT_NAME",
	S.AMOUNT AS "SALARY"
FROM
	PUBLIC.DEPT D,
	PUBLIC.EMP E,
	PUBLIC.SALARY S
WHERE
	(1=1)
	AND E.DEPARTMENT_ID = D.ID AND S.SALARY_ID = S.ID