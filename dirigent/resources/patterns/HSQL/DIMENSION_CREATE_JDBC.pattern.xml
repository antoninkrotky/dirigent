<?xml version="1.0" encoding="UTF-8"?>
<tns:Pattern xmlns:tns="http://www.example.org/dirigent-pattern/"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.example.org/dirigent-pattern/ ../dirigent-pattern.xsd"
	name="DIMENSION_TRUNCATE_INSERT_JDBC">
	<Documentation> Load target table using truncate insert pattern. 
	</Documentation>
	<Parameter name="truncateTargetTable" defaultValue="Yes">
		<Documentation>If Yes target table will be truncated before loading
			data.</Documentation>
	</Parameter>
	<PatternStep name="Drop target table" type="JDBCStatement"
		ignoreErrors="true">
		<Template> DROP TABLE ${element.fullName};</Template>
	</PatternStep>
	<PatternStep name="Create target table" type="JDBCStatement">
		<Template> CREATE TABLE ${element.fullName} (
		${element.codeName}_KEY INTEGER GENERATED BY DEFAULT AS IDENTITY,
		#foreach ($column in $element.columns)
			$column.name $column.dataType,			
		#end
		CREATED_TS TIMESTAMP default CURRENT_TIMESTAMP,
		UPDATED_TS TIMESTAMP default CURRENT_TIMESTAMP
		);
		</Template>
	</PatternStep>
</tns:Pattern>